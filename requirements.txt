# Cloud deployment — only packages actually imported by the server
# (mlx, google-generativeai, numpy, pandas, pymupdf excluded — not used with SKIP_MODEL=1)
fastapi>=0.129.0
pypdf>=6.7.0
requests>=2.32.5
pydantic>=2.0
python-multipart>=0.0.22
python-dotenv>=1.0.0
